{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Core schema meta-schema",
  "description": "Define the structure of how the input variables for the calculator will be received",
  "type": "object",
  "properties": {
    "dividend": {
      "type": "number",
      "description": "dividen"
    },
    "divisor": {
      "type": "number",
      "description": "divisor"
    }
  },
  "required": ["dividen", "divisor"]
}
